{
  "_comment": "Add this block to ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%\\Claude\\claude_desktop_config.json (Windows). The MCP server binary reads from the rust-network-mgr HTTP API.",
  "mcpServers": {
    "network-mgr": {
      "command": "/usr/local/bin/rust-network-mgr-mcp",
      "args": ["--api-url", "http://127.0.0.1:9100"],
      "env": {}
    }
  }
}
